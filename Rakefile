require "rubygems"

# -------------------------------------------------------------------------
#	Tasks for development process
# -------------------------------------------------------------------------
# Usage: rake watch
desc "Build CSS file on save"
task :watch do
  puts "* Watching at _scss"
  system "sass --watch _scss/styles.scss:css/styles.css"	
end